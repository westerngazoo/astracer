/target
**/target
Cargo.lock.bak
*.rs.bk
.DS_Store

# Tauri / frontend build artifacts. `dist/` is ignored whole, placeholder
# included: src-tauri/build.rs writes one when it is missing, so a fresh
# checkout still compiles and git can never restore it over a built bundle.
apps/desktop/frontend/dist/
apps/desktop/src-tauri/gen
node_modules

# Local analysis output
/out
*.livewalk.json
