{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window: invoke commands + listen for engine events.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
