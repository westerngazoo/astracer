/target
**/target
Cargo.lock.bak
*.rs.bk
.DS_Store

# Tauri / frontend build artifacts (keep the checked-in placeholder so the
# backend's generate_context! compiles standalone in a fresh checkout/worktree).
apps/desktop/frontend/dist/*
!apps/desktop/frontend/dist/index.html
apps/desktop/src-tauri/gen
node_modules

# Local analysis output
/out
*.livewalk.json
